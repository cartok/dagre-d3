/* global window */

// var lodash;

// if (require) {
//   try {
//     lodash = require("lodash");
//   } catch (e) {}
// }

// if (!lodash) {
//   lodash = window._;
// }
const lodas = require("loadash")
module.exports = lodash
